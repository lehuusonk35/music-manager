'use strict';

// Register `phoneList` component, along with its associated controller and template
angular.
module('myngappAppApp').
component('songList', {
  templateUrl: 'scripts/song/song.template.html',
  controller: 'SongCtrl'
});
