'use strict';

// Register `phoneList` component, along with its associated controller and template
angular.
module('myngappAppApp').
component('playList', {
  templateUrl: 'scripts/playlist/playlist.template.html',
  controller: 'playListCtrl'
});
